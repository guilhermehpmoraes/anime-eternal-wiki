{
    "version": 2,
    "buildCommand": "pnpx nx build web-client",
    "outputDirectory": "apps/web-client/dist",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ]
}
